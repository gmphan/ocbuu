const Dashboard = () => {
    return (
        <div>
            <h2>Dashboard after login</h2>
            <p>
                I will set something here, so that if they go directly to this
                page without login I will prompt them to login 
                If login then I will start query info relate to the user to display here
            </p>
        </div>
    )
}

export default Dashboard